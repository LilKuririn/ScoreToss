# Clé d'envoi vers le Play Store : ne doit jamais être versionnée.
# (android/debug.keystore est suivi volontairement : il ne signe que
#  l'APK d'installation directe, et son mot de passe est public.)
*.jks
*.b64

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk

# Banc de comparaison : versions de référence extraites de l'historique
.banc/
